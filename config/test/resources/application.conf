camundala {
  basePath = "."
  services {
    host = localhost
    port = 8889
  }
  camunda {
    rest {
      url = "http://localhost:10001/rest"
      user = "kermit"
      password = "kermit"
    }
  }
}
